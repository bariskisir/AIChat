{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the AI Chat window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
