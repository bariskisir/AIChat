{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Claude Chat window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
